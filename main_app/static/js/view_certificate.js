

function viewCertificate(certificate_url){

    // var url = "/viewCertificate";

    // // Construct the full URL with "id"
    // document.location.href = url + "?certificate_url=" + certificate_url;

    document.location.href = certificate_url;
}